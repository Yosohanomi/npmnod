{
  "name": "test",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx parcel src/*.html",
    "build": "parcel build src/*.html --no-cache --public-url /npmnod/",
    "deploy": "gh-pages -d dist",
    "predeploy": "npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "parcel": "^2.16.0"
  },
  "dependencies": {
    "gh-pages": "^6.3.0"
  }
}
